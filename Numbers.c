#include <stdio.h>

void oddOrEven(int n);
void timesTable (int n);
void square (int n);
void prime (int n);

int main(void)

{
    int number;

    printf("what's your number?");
    scanf("%d",&number);

    oddOrEven(number);
    timesTable(number);
    square(number);
    prime(number);

}

void oddOrEven(int n)
{
    return;
}

void timesTable(int n)
{
    return;
}

void square(int n)
{
    return;
}

void prime(int n)
{
    return;
}